{
  "name": "react-stateless-components-ramda",
  "version": "1.0.0",
  "description": "Functional Components with React stateless functions and Ramda",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "python -m http.server 9000",
    "build": "browserify -d -e src/main.js -t babelify > dist/bundle.js",
    "watch": "watchify -v -t babelify src/main.js -o dist/bundle.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.11.1",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "classnames": "^2.2.5",
    "ramda": "^0.21.0",
    "react": "^15.2.0",
    "react-dom": "^15.2.0",
    "watchify": "^3.7.0"
  }
}
